package hanifi.siavash.iv1350.processSale.model;

import hanifi.siavash.iv1350.processSale.data.SaleDTO;
/**
 * Representation of a receipt in a format which is readable by the printer-system.
 * (Since the format is dependent on the API of the printer (which we do not have 
 * 	any information of), the member attributes and methods were not designed for
 *  this class.)
 * @author Siavash
 */
public class Reciept {

	public Reciept(SaleDTO loggedSale, Payment payment, Change change) {
		
	}

}
